# Example Environment Variables
# Copia este archivo a .env.local y rellena con tus valores

# ============================================
# Firebase Client Configuration (OPCIONAL)
# ============================================
# Si no configuras estas variables, las características de autenticación
# estarán deshabilitadas pero la app funcionará normalmente

# NEXT_PUBLIC_FIREBASE_API_KEY=
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
# NEXT_PUBLIC_FIREBASE_APP_ID=
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# ============================================
# Firebase Admin Configuration (OPCIONAL)
# ============================================
# Base64 encoded Firebase Admin SDK JSON key
# Obtén esto desde Firebase Console > Project Settings > Service Accounts
# Para encodear: cat serviceAccountKey.json | base64 -w 0

# FIREBASE_ADMIN_SDK=

# ============================================
# MongoDB Configuration (OPCIONAL)
# ============================================
# Si no configuras esto, el historial de predicciones no se guardará
# pero la app seguirá funcionando normalmente

# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/

# ============================================
# Seguridad y CSRF (RECOMENDADO)
# ============================================
# Generar con: openssl rand -hex 32
NEXT_PUBLIC_JWT_SECRET=tu-clave-secreta-muy-segura-cambiar-en-produccion

# ============================================
# URL de la Aplicación (RECOMENDADO)
# ============================================
# Usado para validar redirects y CORS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Rate Limiting (OPCIONAL)
# ============================================
# Máximo de intentos de login en 15 minutos
NEXT_PUBLIC_MAX_LOGIN_ATTEMPTS=5
NEXT_PUBLIC_LOGIN_ATTEMPT_WINDOW=900000

# ============================================
# API Base URL (OPCIONAL)
# ============================================
# Para servicios externos como mel_spectrogram
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# ============================================
# Notas Importantes
# ============================================
# 1. NEXT_PUBLIC_* son variables que se envían al cliente (públicas)
# 2. Variables sin prefix se mantienen privadas en el servidor
# 3. Si alguna variable no está configurada, la feature correspondiente
#    será deshabilitada automáticamente
# 4. Verifica los logs para mensajes de (warnings) cuando algo no esté configurado
# 5. NUNCA commites .env.local con datos reales a GitHub
# 6. Cambiar NEXT_PUBLIC_JWT_SECRET en producción
# 7. Usar HTTPS en producción siempre
